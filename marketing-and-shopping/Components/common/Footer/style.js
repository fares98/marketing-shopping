import { StyleSheet } from "react-native";

const footer = StyleSheet.create({
  parent: {
    height: 40
  },

  text: {
    fontSize: 16,
    textAlign: "center",
    lineHeight: 40,
    fontWeight: "bold"
  }
});

export default footer;
