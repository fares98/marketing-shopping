import { StyleSheet } from "react-native";

const upload = StyleSheet.create({
  parent: {
    height: 40,
    marginTop: 30,
    borderRadius: 5
  },

  text: {
    textAlign: "center",
    fontSize: 16,
    lineHeight: 40
  }
});

export default upload;
