import { StyleSheet } from "react-native";

const button = StyleSheet.create({
  parent: {
    height: 40,
    width: 200,
    borderRadius: 5,
    marginTop: 30
  },

  text: {
    fontSize: 16,
    textAlign: "center",
    lineHeight: 40
  }
});

export default button;
