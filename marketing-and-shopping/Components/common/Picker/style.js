import { StyleSheet } from "react-native";

const picker = StyleSheet.create({
  parent: {
    height: 40,
    marginTop: 30,
    borderRadius: 5
  },

  item: {
    backgroundColor: "red",
    width: 10
  }
});

export default picker;
